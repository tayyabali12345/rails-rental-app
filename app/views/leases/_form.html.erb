<%= form_with(model: lease) do |form| %>
  <% if lease.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(lease.errors.count, "error") %> prohibited this lease from being saved:</h2>
      <ul>
        <% lease.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :Select_User %><br />
    <%= form.collection_select :user_id, User.all, :id, :name %>
  </div>
  <div class="field">
    <%= form.label :Select_Property %><br />
    <%= form.collection_select :property_id, Property.all, :id, :property_type %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
